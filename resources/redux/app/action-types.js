const baseType = "APP";

const types = {
    SET_ACADEMIC_YEAR: `${baseType}/SET_ACADEMIC_YEAR`,
    SET_CURRENT_GROUP: `${baseType}/SET_CURRENT_GROUP`,
    LOGOUT: `${baseType}/LOGOUT`,
};

export default types;
